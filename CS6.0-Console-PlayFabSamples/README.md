# PlayFab C# SDK Samples

# How to use
- `local.settings.json`, `appsettings.json` が必要。`local.settings.json.copy` と `appsettings.json.copy` からコピーして作成する。
- playfab.local.settings.json は ローカルデバッグが必要なときに、%TEMP% にコピーする。

